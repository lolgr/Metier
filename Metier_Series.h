#pragma once
#include "Metier_Chart.h"
#include <functional>

#ifndef SERIES_H
#define SERIES_H

// class Chart;

// template <typename T> class Series {
//     public:
//         T* data;

//         /*Series, (data[0], 
//             funtion, 
//             chart, 
//             start, //number of bars back or length so 100 to start 100 bars back
//             end) () //numbers of bars back so 0 for until recent
            
//         */
//         Series(T data, std::function<T(Chart*, int, int)> method, Chart* chart, int start, int end) {

//         }

//         // Series(T data, std::function<T()> method, )

// };

#endif
